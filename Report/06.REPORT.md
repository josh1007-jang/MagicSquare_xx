# MagicSquare_xx — D-LOC-01 GREEN Minimal

| 항목 | 내용 |
|------|------|
| **프로젝트** | MagicSquare_xx |
| **단계** | GREEN (Entity D-LOC-01) |
| **Track** | Logic (Entity) |
| **보고서 생성일** | 2026-06-10 |
| **목적** | RED skeleton `test_d_loc_01_blank_coords_row_major` GREEN — `src/entity/` `find_blank_coords` + `entity/constants.py` SSOT |

---

## 1. 요약

Export 05에서 추가한 **D-LOC-01** RED skeleton(`pytest.fail` only)을 `/green-minimal`로 GREEN했다. `src/entity/constants.py`를 Entity SSOT로 신설하고 `src/entity/find_blank_coords.py`에 `BLANK` 기반 row-major 1-indexed 빈칸 스캔을 구현했다. `src/find_blank_coords.py`는 entity 모듈 re-export로 기존 import 경로를 유지했다. skeleton의 `pytest.fail`을 실제 assert로 교체하고, Then 주석·기대값을 G1 실제 좌표 `[(2,3),(4,4)]`로 정렬했다(스켈레톤 오기 `[(2,2),(3,3)]` 수정). 전체 pytest **8 passed**.

---

## 2. 핵심 결정·산출물

| 구분 | 내용 |
|------|------|
| Phase | GREEN — Entity D-LOC-01 |
| 변경·생성 파일 | `src/entity/constants.py` (created), `src/entity/find_blank_coords.py` (created), `src/find_blank_coords.py` (updated — re-export), `tests/entity/test_d_loc_01.py` (updated — assert 교체) |
| pytest (D-LOC-01) | `python -m pytest tests/entity/test_d_loc_01.py -v` → **2 passed** |
| pytest (Boundary) | `python -m pytest tests/test_validate_lines.py -v` → **5 passed** |
| pytest (전체) | `python -m pytest -v` → **8 passed** |
| API·계약 | `find_blank_coords(grid) -> list[tuple[int,int]]` — `0` 위치 1-indexed row-major; G1 → `[(2,3),(4,4)]` |
| ECB | Entity는 boundary/control import 없음; E001~E005 emit 없음 |
| REPL 스모크 | G1 → `[(2, 3), (4, 4)]` |

### GREEN 구현 요약

| 항목 | 내용 |
|------|------|
| SSOT | `src/entity/constants.py` — `GRID_SIZE=4`, `MAGIC_CONSTANT=34`, `MAX_CELL=16`, `BLANK=0` |
| 핵심 로직 | `value == BLANK` → `(row_idx+1, col_idx+1)` row-major 수집 |
| 하드코딩 | `0`·`4` 매직넘버 제거 — `BLANK` 상수 사용 |

---

## 3. pytest 결과

```bash
python -m pytest tests/test_validate_lines.py -v
```

```
5 passed in 0.02s
```

```bash
python -m pytest tests/entity/test_d_loc_01.py -v
```

```
2 passed in 0.03s
```

```bash
python -m pytest -v
```

```
8 passed in 0.04s
```

---

## 4. 다음 단계

- **Logic RED:** D-LINE-C2-01 (C2 열 fail) — `tests/test_validate_lines.py`
- **REFACTOR:** `validation.py`·`solve_step_a`의 `MAGIC_CONSTANT`/`4` → `entity/constants.py` SSOT 통합 (별도 사이클)
- **Git:** GREEN 변경 commit & push (사용자 요청 시)

---

**관련 Transcript:** [Prompting/06.Export-Transcript.md](../Prompting/06.Export-Transcript.md)

*본 문서는 Report/06.REPORT.md — D-LOC-01 GREEN Minimal 세션 Export입니다.*
