# MagicSquare_xx — 세션 3 워크북

**작성일:** 2026-06-10  
**프로젝트:** MagicSquare_xx  
**입력:** Mom Test STEP 1 (`Report/MomTest_STEP1_MagicSquare_xx.md`)

---

## Mom Test 결과 (요약)

| 항목 | 내용 |
|------|------|
| **페르소나** | 4×4 부분 마방진을 손으로/코드로 다루는 학습자 |
| **진짜 문제 (한 문장)** | 4×4 부분 마방진을 손으로 채운 뒤 행·열·대각선 합을 맞췄다고 믿었지만, **확인 항목 하나를 빼먹어 틀린 상태로 20분을 쓴 뒤에야** 잘못을 알 수 있다. |
| **Mom Test 증거 3줄** | ① "첨부한거 같이 **어려움이 있었어**" ② "첨부한거 같이 **작업했는데 어려움이 있었어**" ③ "**빈칸 2개 넣고 행·열·대각선 합 맞췄는데 대각선 하나를 빼먹어서 20분 날렸어**" |

---

## 1) 주제 한 문장 (Mom Test 기반, 솔루션 최소화)

> 행·열·대각선 합 확인에서 **항목을 하나 빼먹어** 틀린 완성 상태를 오래 유지하는 불편을, **빠짐없는 검증**으로 조기에 드러낸다.

---

## 2) R-G-I-O

| | 내용 |
|---|------|
| **Role** | 4×4 부분 마방진 실습 학습자 — 빈칸 2개를 채우고 목표 합 34를 손·코드로 검증한다. |
| **Goal** | 행 4개·열 4개·대각선 2개 **총 10개 항목**을 모두 확인했을 때만 완성으로 판정하고, 하나라도 34가 아니면 **어느 항목**인지 즉시 알 수 있다. |
| **Input** | 4×4 정수 격자 (0=빈칸, 1–16, 빈칸 2개), 목표 합 = 34 |
| **Output** | 10개 항목별 합 + pass/fail + 실패 시 해당 행/열/대각선 식별 |

---

## 3) 성공 기준 3개 (Mom Test 증거와 연결)

| # | 성공 기준 | 연결 증거 |
|---|-----------|-----------|
| 1 | 검증 범위에 **대각선 2개가 명시·실행**되며, 행·열만 맞고 대각선 하나만 틀린 격자에서 **fail + 실패 대각선 식별**이 나온다. | 증거 ③ "대각선 하나를 빼먹어서" |
| 2 | ECB·실습과 동일 맥락(빈칸 2개, 합 34, 첨부 슬라이드 과제)의 **재현 시나리오**가 Test Loop에 포함된다. | 증거 ①·② "어려움이 있었어" / "작업했는데 어려움이 있었어" |
| 3 | 행·열만 확인한 **부분 검증**으로는 "완성" 판정을 내릴 수 없고, 전체 10항목 1회 검증으로 틀림을 특정한다 (20분 착각 재현 방지). | 증거 ③ "20분 날렸어" |

---

## 4) 표면 문제 — 이번 프로젝트에서 하지 않을 것

Mom Test에서 기록만 하고 **이번 세션 범위 밖**으로 둔다.

| 표면 문제 (금지) | 이번에 하지 않음 |
|------------------|------------------|
| "4×4 부분 마방진 **검증 프로그램**을 만들면 좋겠어." | 독립 앱·프로그램 구현 |
| "행·열·대각선 합을 **자동으로 체크해 주는 도구**가 필요해." | UI/자동화 **제품**으로 정의·개발 |
| "ECB 구조로 **마방진 솔버 앱**을 만들면 실습이 쉬워질 거야." | 솔버·빈칸 **자동 채우기** |
| (암묵) | 8계층 중 **Rule · Command · (Skill) · Test Loop** 외 계층(전체 Agent, 배포, ECB 분류 도구 등) |

---

## 8계층 — 이번 세션에서 만드는 것

| 계층 | 세션 3 | 이번에 만드는 것 |
|------|:------:|------------------|
| Mom Test / 페르소나 | — | STEP 1 완료 (입력만 사용) |
| 주제 · R-G-I-O | — | 본 워크북 (설계만) |
| **Rule** | ✅ | **검증 체크리스트 규칙** — 행4·열4·대각선2, 합34; 항목 하나라도 빠지면 "완성" 금지; 실패 시 항목명 보고 |
| **Command** | ✅ | **격자 입력 → 10항목 검증 실행** 트리거 (예: `/verify-magic-square` 또는 동등 프롬프트) |
| **Skill** | (선택) | 필요 시 **마방진 검증 워크플로** Skill — Rule+Command를 에이전트가 반복 실행 |
| **Test Loop** | ✅ | **TC1** 전 항목 pass / **TC2** 행·열 OK·대각선 1개 fail(인터뷰 재현) / **TC3** 대각선 검증 생략 시 Rule 위반 감지 |
| Agent / 구현 / 배포 | — | 이번 세션 범위 밖 |

---

## 다음 세션 힌트

- Q3 미응답("언제·무엇을 보고 틀림을 알았는지") 보완 후 Test Loop **실패 메시지** 문구 검증
- Rule·Command·Test Loop 구현 시 성공 기준 3개를 **통과/실패**로 매핑
